test_process =>

  op := fuzion.sys.process.create "cat"
  match op
    p fuzion.sys.process.r_process =>
      p.write "Hello from the other side."
      say p.close_write
      say (p.read 100)
      # NYI
      # p.wait
    error => say "error"
